#!/bin/bash

# Portfolio Risk App Bundle Launcher
# Переходим в директорию проекта и запускаем приложение

# Находим путь к корневой папке проекта
APP_DIR="$(dirname "$0")"
PROJECT_DIR="$(cd "$APP_DIR/../../../" && pwd)"

echo "🚀 Запуск Portfolio Risk из App Bundle..."
echo "Проект: $PROJECT_DIR"

# Переходим в проект и запускаем
cd "$PROJECT_DIR"
./start.command 